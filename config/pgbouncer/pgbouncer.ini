#pgbouncer.ini

[databases]
* = host = moodledb port=5432 user=moodle password=m@0dl3ing

[pgbouncer]
listen_addr = 0.0.0.0
auth_type = any
ignore_startup_parameters = extra_float_digits

# Log settings
admin_users = postgres

# Connection sanity checks, timeouts

# TLS settings

# Dangerous timeouts

